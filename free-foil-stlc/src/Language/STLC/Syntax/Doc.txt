The Language Syntax
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Syntax==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.





Var literals are recognized by the regular expression
`````lower (char - ["
 !'(),.:;?[]{|}⟦⟧"])*`````

ModuleName literals are recognized by the regular expression
`````upper (char - ["
 !'(),.:;?[]{|}⟦⟧"])*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Syntax are the following:
  | ``Int`` | ``as`` | ``export`` | ``from``
  | ``import`` | ``module`` | ``where`` |

The symbols used in Syntax are the following:
  | * | { | } | ,
  | : | = | #typecheck | <=
  | #typesynth | => | ; | !
  | ? | ( | ) | \
  | . | + | # | ->
  | |- |  |  |

===Comments===
Single-line comments begin with --.There are no multiple-line comments in the grammar.

==The syntactic structure of Syntax==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Statement]//
  | //ImportItem// | -> | ``*``
  |  |  **|**  | ``*`` ``as`` //ModuleName//
  |  |  **|**  | ``{`` //[ImportItemName]// ``}``
  | //ImportItemName// | -> | //ModuleName//
  |  |  **|**  | //Var//
  | //[ImportItemName]// | -> | **eps**
  |  |  **|**  | //ImportItemName//
  |  |  **|**  | //ImportItemName// ``,`` //[ImportItemName]//
  | //Location// | -> | //ModuleName//
  |  |  **|**  | //String//
  | //Statement// | -> | ``module`` //ModuleName// ``where`` ``{`` //[Statement]// ``}``
  |  |  **|**  | ``import`` //ImportItem// ``from`` //Location//
  |  |  **|**  | //Var// ``:`` //Type//
  |  |  **|**  | //Var// ``=`` //Exp//
  |  |  **|**  | //Var// ``=`` //Exp// ``where`` ``{`` //[Statement]// ``}``
  |  |  **|**  | ``export`` //[ExportItem]//
  |  |  **|**  | ``#typecheck`` //ExpUnderCtx// ``<=`` //Type//
  |  |  **|**  | ``#typesynth`` //ExpUnderCtx// ``=>`` //SynthResult//
  | //ExportItem// | -> | //ModuleName//
  |  |  **|**  | //Var//
  | //[ExportItem]// | -> | **eps**
  |  |  **|**  | //ExportItem//
  |  |  **|**  | //ExportItem// ``,`` //[ExportItem]//
  | //[Statement]// | -> | **eps**
  |  |  **|**  | //Statement// ``;`` //[Statement]//
  | //SynthResult// | -> | ``!`` //Type//
  |  |  **|**  | ``?``
  | //Exp1// | -> | ``(`` //Exp// ``)`` //Exp//
  |  |  **|**  | //Exp2//
  | //Exp2// | -> | ``\`` //Var// ``.`` //Exp//
  |  |  **|**  | //Exp3//
  | //Exp3// | -> | //Exp3// ``+`` //Exp4//
  |  |  **|**  | //Exp4//
  | //Exp4// | -> | //Var//
  |  |  **|**  | //Exp5//
  | //Exp5// | -> | //Integer//
  |  |  **|**  | //Exp6//
  | //Exp6// | -> | ``#`` //[Accessor]//
  |  |  **|**  | ``(`` //Exp// ``)``
  | //Exp// | -> | //Exp1//
  | //Accessor// | -> | //ModuleName//
  |  |  **|**  | //Var//
  | //[Accessor]// | -> | **eps**
  |  |  **|**  | //Accessor//
  |  |  **|**  | //Accessor// ``.`` //[Accessor]//
  | //Type1// | -> | ``Int``
  |  |  **|**  | //Type2//
  | //Type2// | -> | //Type// ``->`` //Type//
  |  |  **|**  | ``(`` //Type// ``)``
  | //Type// | -> | //Type1//
  | //CtxVar// | -> | //Var// ``:`` //Type//
  | //[CtxVar]// | -> | **eps**
  |  |  **|**  | //CtxVar//
  |  |  **|**  | //CtxVar// ``,`` //[CtxVar]//
  | //Ctx// | -> | //[CtxVar]//
  | //ExpUnderCtx// | -> | //Ctx// ``|-`` //Exp//



%% File generated by the BNF Converter (bnfc 2.9.6).
